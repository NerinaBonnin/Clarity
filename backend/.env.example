PORT=5000
JWT_SECRET=cambia_esto_por_un_secreto_largo

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=mylist

# Mismo Client ID que en el frontend (Google Cloud Console)
GOOGLE_CLIENT_ID=

# Services ID de Apple Developer (Sign in with Apple)
APPLE_CLIENT_ID=
